name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 20
          cache: npm
      - run: npm run check
      - run: npm run package
      - uses: actions/upload-artifact@v7
        with:
          name: chatgpt-bridge-packages
          path: dist/*.zip
